spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/tickets_shop_test
    username: test_user
    password: test_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true

  flyway:
    enabled: false
